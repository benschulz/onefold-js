'use strict';

define(['./arrays', './functions', './objects', './strings'], function (arrays, functions, objects, strings) {
    return {
        arrays: arrays,
        functions: functions,
        objects: objects,
        strings: strings
    };
});
